@props(['value', 'label'])

<div class="text-center">
    <div class="text-3xl font-extrabold text-white sm:text-4xl">{{ $value }}</div>
    <div class="mt-1 text-sm font-medium text-primary-100">{{ $label }}</div>
</div>
