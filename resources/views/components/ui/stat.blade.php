@props(['value', 'label'])

<div>
    <div class="font-display text-4xl font-semibold tracking-tight text-ink-900 sm:text-5xl">{{ $value }}</div>
    <div class="mt-2 text-sm text-slate-500">{{ $label }}</div>
</div>
